@typeparam TElemento


@*@if ( Listado == null)
{
    if (ListadoNulo == null)
    {
        <p>Cargando...</p>
    }
    else
    {
        @ListadoNulo
    }
}
else if (Listado.Count == 0)
{
    if (ListadoVacio == null)
    {
        <p>No hay registros en la base de datos</p>

    }
    else
    {
        @ListadoVacio
    }
}
else
{*@
   @ListadoConElementos   
@*}*@

@code {

    [Parameter] public List<TElemento> Listado { get; set; }
    [Parameter] public string Titulo { get; set; }
    [Parameter] public RenderFragment ListadoNulo { get; set; }
    [Parameter] public RenderFragment ListadoVacio { get; set; }
    [Parameter] public RenderFragment ListadoConElementos { get; set; }

}
